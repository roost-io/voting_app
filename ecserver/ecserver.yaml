apiVersion: v1
kind: Service
metadata: 
  labels: 
    apps: ecserver
  name: ecserver
spec: 
  type: NodePort
  selector: 
    app: ecserver
  ports:
  - name: ecserver
    port: 80
    targetPort: 8081
    # nodePort: 30081
  type: LoadBalancer

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ecserver
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ecserver
  template:
    metadata:
      labels:
        app: ecserver
    spec:
      containers:
      - name: ecserver
        image: vivekklp/ecserver:latest
        ports:
        - containerPort: 8081
